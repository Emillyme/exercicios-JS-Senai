let contador = 20
let limite = 50

while(contador <= limite){
    console.log(`Número atual: ${contador}`)
    contador++
}

let impar = 100
do{
    if (impar % 2 != 0){
        document.write(`${impar} <br>`)
    }
    impar++
}while(impar <= 150);